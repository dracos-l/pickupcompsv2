import React from 'react';
import DropdownMenu from './DropdownMenu';


const Header = () => {
  return (
    <header>
      <h1>(HOME BUTTON/LOGO)</h1>
      <nav>
        <DropdownMenu />
      </nav>
    </header>
  );
}

export default Header;
